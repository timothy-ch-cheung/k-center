%TC:ignore
\begin{algorithm}[H] 
\caption{Colourful PBS Algorithm}
\label{alg:colourful_pbs}
\begin{algorithmic}[1]
\Procedure{PBS}{}
    \State{seed\textunderscore solution$\gets$ approx\textunderscore colourful()}\Comment{\emph{Ban} algorithm described in \cref{section:constant_colourful_k_center} \cref{alg:approx_colourful}\hphantom{...............................................}}
    \State {C $\gets$\{seed\textunderscore solution\}}
    \For{$i \gets 1$ to population\textunderscore size}
        \State {f $\gets$ SelectRandom(V); $C_i\gets$ ColourfulLocalSearch({f})}
    \EndFor
    \Repeat
        \For{$C_i \in C$}
            \For{$C_j \in C$}
                \State{$C$ $\gets C$ $+$ ColourfulLocalSearch($M_1(C_j)$)}
                \State{$C$ $\gets C$ $+$ ColourfulLocalSearch($M_2(X_1 (C_i,C_j))$)}
                \State{$(S_1,S_2)\gets X_2(C_i,C_j)$}
                 \State{$C$ $\gets C$ $+$ ColourfulLocalSearch($M_2(S_1)$)} $+$ ColourfulLocalSearch($M_2(S_2)$)
            \EndFor    
        \EndFor
    \Until{termination condition}
\EndProcedure
\end{algorithmic}
\end{algorithm}
%TC:endignore